<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cryptapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CryptApp">
        <activity android:name=".ResetKey"
            android:label="Reset Key"
            android:parentActivityName=".AES"/>
        <activity
            android:name=".MD5"
            android:label="MD5 Algorithm"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".RSA"
            android:label="RSA Encryption"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DES"
            android:label="DES Encryption"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AES"
            android:label="AES Encryption"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>